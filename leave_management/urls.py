from django.conf.urls import url
# from leave_management.views import AddLeaveType,LeaveList,SetLeaveAPI
urlpatterns = [
    # url(r'^add_leavetype/',AddLeaveType.as_view(), name='add_leavetype'),
    # url(r'^leave_list/',LeaveList.as_view(), name='leave_list'),
    # url(r'^set_leave/',SetLeaveAPI.as_view(), name='set_leave'),
]
